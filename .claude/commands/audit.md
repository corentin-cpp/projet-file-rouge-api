Lance un audit complet de sécurité et qualité sur l'API.

Si un fichier ou dossier est passé en argument, auditer uniquement ce périmètre. Sinon auditer tout `src/` et `migrations/`.

Utilise le sous-agent `audit` pour effectuer l'analyse et rendre un rapport structuré par niveau de criticité (Critique / Majeur / Mineur / OK).
